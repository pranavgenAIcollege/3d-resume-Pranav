{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx vite src --host 0.0.0.0",
    "build": "npx parcel build src/index.html --public-url ./",
    "format": "npx prettier . --write"
  },
  "dependencies": {
        "@xenova/transformers": "^2.6.1",
        "three": "^0.156.1"
  },
  "devDependencies": {
        "prettier": "3.0.3",
        "process": "^0.11.10",
        "stats.js": "^0.17.0",
        "vite": "^4.4.9"

  }
}
